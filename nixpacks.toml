[phases.setup]
    nixPkgs = ["nodejs_20"]

[phases.install]
    cmds = ["npm ci --include=dev"]

[phases.build]
    cmds = [
        "npm run build",
        "npx prisma generate"
    ]

[phases.start]
    cmd = "bash scripts/railway-startup.sh"