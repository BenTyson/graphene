[phases.setup]
    nixPkgs = ["nodejs_20"]

[phases.build]
    cmds = [
        "npm install",
        "npx prisma generate",
        "npx prisma db push"
    ]

[phases.start]
    cmd = "npm start"

[staticAssets]
    "/client" = "./client"