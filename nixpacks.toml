[phases.setup]
    nixPkgs = ["nodejs_20"]

[phases.build]
    cmds = [
        "npm install",
        "npm run build",
        "npx prisma generate"
    ]

[phases.start]
    cmd = "npx prisma db push && npm start"