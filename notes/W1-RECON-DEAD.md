# CHIP-W1-RECON-DEAD

- **Lane:** B (spec/docs/research)
- **Model tier used:** sonnet
- **Owned files:** `notes/W1-RECON-DEAD.md`
- **Wave:** 1 (re-run)
- **Base:** `staging` @ `c9486de`

## What I was asked to do

Mechanical dead-code recon across a list of candidates (root-level app.js/app-original.js,
DealModal/DealDetailPanel, root test-*.js scripts, root strays, docs/archive/, constants.js
TASK_CATEGORY_TAGS contradiction, criticalBlock/advancedAccordion proforma helpers, unused
exports generally). I run the four-search protocol (§7) per candidate and tabulate evidence.
I do not decide removal — the Command Center rules on that. I do not edit any source file.

Note: `client/src/js/app-refactored.js`, `server/index.js`, `server/routes/auth.js`,
`package.json`, `scripts/check/**`, and `client/src/js/data/testMatrix.js` are being actively
rewritten by sibling chips concurrently (W1-APP-DEDUPE at minimum owns app-refactored.js). Any
grep result touching those files is a snapshot as of when I ran the command, not a stable fact.

## Status log (appended as I work)

- [started] notes skeleton created, beginning candidate 1
- [done] all 8 candidates researched, evidence table complete, Reflections written

## Evidence table

| Candidate | Type | Basename hits | Path hits | Dynamic refs | HTML refs | Verdict-ready? | Evidence |
|---|---|---|---|---|---|---|---|
| `client/src/js/app.js` (828 lines, 27184 bytes) | orphan file | none outside self/docs | none | none | not in index.html script tags | **unreferenced — all four searches clean** | `grep -rn "app\.js" --include="*.js" --include="*.html" --include="*.md" --include="*.json" . \| grep -v node_modules \| grep -v dist/ \| grep -v app-refactored.js \| grep -v app-original.js` → only `docs/archive/phase1.md:369`, `docs/archive/REFACTORING.md:4,136` (historical narrative, not references). `client/index.html:1349` loads `/src/js/app-refactored.js` only — confirmed by `grep -n "app" client/index.html`. No `import`/`from` statement anywhere (`grep -rn "from ['\"].*app['\"]" client server shared` empty). Not in `vite.config.js` `input` block. |
| `client/src/js/app-original.js` (828 lines, 27184 bytes, byte-identical to app.js — `diff` returns nothing) | orphan file | none outside self/docs/roadmap | none | none | not in index.html | **unreferenced — all four searches clean** | `grep -rn "app-original" --include="*.js" --include="*.html" --include="*.md" --include="*.json" .` → `ROADMAP.md:128` and `CHIP-PROTOCOL.md:171` (both are *about this same investigation*, not code references), plus `docs/archive/phase1.md:239`, `docs/archive/REFACTORING.md:12,109` (historical narrative saying it's "a backup"). `diff client/src/js/app.js client/src/js/app-original.js` → identical, confirming it really is a backup copy per the archive doc's own claim. |
| `client/src/js/components/modals/DealModal.js` (89 lines, 5152 bytes) | orphan module, exports `getDealModalHtml()` | only its own `export function` line | none | none | no `Deal` string anywhere in `client/index.html` (`grep -n -i "deal" client/index.html` empty) | **unreferenced — all four searches clean** | `grep -rn "DealModal" --include="*.js" --include="*.html" .` and `grep -rn "getDealModalHtml" --include="*.js" --include="*.html" .` both return only the file's own definition line. Confirms CLAUDE.md's own claim that this is dead code (CLAUDE.md Gotchas: "`DealModal.js` and `DealDetailPanel.js` are dead code (not imported)"). |
| `client/src/js/components/modals/DealDetailPanel.js` (160 lines, 10778 bytes) | orphan module, exports `getDealDetailPanelHtml()` | only its own `export function` line | none | none | no `Deal` string in index.html | **unreferenced — all four searches clean** | Same commands as DealModal.js, same result — only self-hit. `grep -rn "DealService"` also empty (no partial service either). Matches CLAUDE.md's documented claim; documentation is correct here. |
| 12 of the 14 root `test-*.js` scripts: `test-all-modal-components.js` (368L), `test-app-functionality.js` (326L), `test-bet-component.js` (120L), `test-bet-modal-component.js` (249L), `test-conductivity-component.js` (189L), `test-conductivity-modal-component.js` (282L), `test-crud-extraction.js` (217L), `test-filter-extraction.js` (80L), `test-raman-component.js` (287L), `test-sem-reports-component.js` (226L), `test-tem-component.js` (221L), `test-update-reports-component.js` (265L) | standalone ad-hoc scripts | no hits outside filesystem listing | none | none (not applicable, no HTML) | **ambiguous — needs a human**: zero references in `package.json` scripts, zero mentions in any doc, zero mentions in any other source file. But these are meant to be run directly via `node <file>.js` (CLI entrypoints), not imported — absence-of-import is expected for this file *shape*, so it is weaker evidence than for an ES module like DealModal. DECISIONS.md D-007 already rules: "The root-level `test-*.js` files are ad-hoc one-off scripts, not a test suite. Do not cite them as verification." That establishes they're not a test *suite*, but does not establish any individual one is safe to delete. | `for f in <list>; do grep -rln "$f" --include="*.js" --include="*.md" --include="*.json" --include="*.html" . \| grep -v node_modules \| grep -v dist/ \| grep -v -- "^\./$f\$"; done` → empty for all 12. `grep -n "test-" package.json` under scripts → empty (no npm script runs any of them). |
| 2 of 14 root `test-*.js`: `test-graphene-filter.js` (159L), `test-openai.js` (81L) | standalone ad-hoc scripts, documented | no code hits | none | n/a | **REFERENCED — do not remove**: cited as manual dev commands in live (non-archive) docs. | `docs/features/NEWS-FILTERING.md:51` (`node test-graphene-filter.js`) and `:103` ("Monthly Review Tasks: Run `node test-graphene-filter.js`"); `docs/features/NEWS-SYSTEM.md:264` (`node test-openai.js  # Tests API key and generates sample summary`). Both also appear in `docs/archive/` copies of the same docs (expected duplication, see candidate 5). |
| `cleanup-non-graphene-articles.js` (8780 bytes, root) | standalone ad-hoc DB script | only self-hit | none | none | none | **ambiguous — needs a human**: `grep -rln "cleanup-non-graphene-articles" --include="*.js" --include="*.md" --include="*.json" .` → only itself. Not in `package.json`, not in any doc (`grep -rln "non-graphene" docs/ README.md` → only doc files about the *filter*, not this script by name). It performs live Prisma writes (`DELETE`-style cleanup) when run — per D-004/D-005 I cannot execute it to check further, and shouldn't. Undocumented but functional-looking one-off maintenance tool; can't distinguish "abandoned" from "manually run outside the repo's docs" without asking a human who ran it. |
| `generate-pending-summaries.js` (6217 bytes, root) | standalone ad-hoc script | only self-hit | none | none | none | **ambiguous — needs a human**: `grep -rln "generate-pending-summaries" --include="*.js" --include="*.md" --include="*.json" .` → completely empty, not even itself matched with the extension filter oddity aside — zero hits anywhere including docs. Uses `SummaryService` from `graphene-news/backend/services/SummaryService.js`; would need to confirm that service itself is still live before judging this dead (out of my search scope this pass). |
| `article-backup-2025-09-09T23-10-49.json` (280208 bytes), `article-backup-2025-09-09T23-18-55.json` (172653 bytes) | stale output artifacts | referenced only via a *pattern* inside `cleanup-non-graphene-articles.js:51`, which dynamically constructs `article-backup-${timestamp}.json` — it does not name these specific files | none | none | **unreferenced — but they are DATA not CODE**: no file in the repo names these two specific filenames; `cleanup-non-graphene-articles.js` generates a *new* differently-named backup on every run, so these are one-time run artifacts from 2025-09-09, not something re-read by any script. | `grep -rln "article-backup" --include="*.js" --include="*.md" .` → only `cleanup-non-graphene-articles.js` (the pattern-generator, not a reader). Safe-to-archive candidate (they're just historical snapshots), but not source code — different removal risk profile than the JS candidates above. |
| `staging-seed-data.json` (697632 bytes, root) | live data file | n/a (data file) | n/a | n/a | n/a | **REFERENCED — do not remove**: read by `server/routes/seed-staging.js:29` (`path.join(__dirname, '..', '..', 'staging-seed-data.json')`) and written by `scripts/export-data.js:54`. Live round-trip data file, not a stray. | `grep -rln "staging-seed-data" .` → `server/routes/seed-staging.js`, `scripts/export-data.js`. |
| `staging_data.sql` (492386 bytes, root) | live data file | n/a | n/a | n/a | **REFERENCED — do not remove**: read by `scripts/import-to-staging.js:16` (`const SQL_FILE = path.join(__dirname, '..', 'staging_data.sql');`). | `grep -rln "staging_data.sql" .` → `scripts/import-to-staging.js`. |
| `docs/archive/` — 10 of 14 files: `CHARACTERIZATION-ANALYSIS-V2.md`, `CLAUDE-AI.md`, `CLAUDE-ARCHITECTURE.md`, `CLAUDE-NEWS.md`, `DEPLOYMENT-WORKFLOW.md`, `DEVELOPMENT-WORKFLOW.md`, `OPTIMIZATION_FINAL_REPORT.md`, `OPTIMIZATION_PROGRESS.md`, `OPTIMIZATION_TEST_CHECKLIST.md`, `graphene-filter-summary.md` | doc duplicate | n/a | n/a | n/a | **byte-identical to a live `docs/` counterpart — but the counterpart is itself unlinked from `docs/README.md`'s index (see finding below)** | `diff docs/archive/<f> docs/<f>` → empty (identical) for all 10. `docs/CHARACTERIZATION-ANALYSIS-V2.md` is additionally byte-identical to `docs/features/CHARACTERIZATION-ANALYSIS.md` (`diff docs/CHARACTERIZATION-ANALYSIS-V2.md docs/features/CHARACTERIZATION-ANALYSIS.md` → empty) — a **triple**-duplicate (archive / docs-root / docs-features). None of the other 9 flat `docs/<f>` copies are linked from `docs/README.md` (`grep -c "<f>" docs/README.md` → 0 for all 9), while `docs/README.md`'s own index points to differently-named files under `docs/{core-reference,workflows,features,history}/` for the same subjects (e.g. `ARCHITECTURE.md`, `DEVELOPMENT.md`, `DEPLOYMENT.md`, `NEWS-SYSTEM.md`, `NEWS-FILTERING.md`). So both the archive copy AND the flat `docs/` copy of these 9 look like leftovers from before a docs reorg, not "one live + one archived." |
| `docs/archive/CLAUDE.md` (2374 bytes) | doc duplicate | n/a | n/a | n/a | **REFERENCED indirectly** — identical to `docs/CLAUDE.md` (`diff` empty) but wildly different from the real, actively-maintained root `CLAUDE.md` (`diff docs/archive/CLAUDE.md CLAUDE.md` → 226 diff lines). `docs/CLAUDE.md` is not linked from `docs/README.md` either. This is a stale pre-restructure snapshot, not the canonical CLAUDE.md. | `diff docs/archive/CLAUDE.md docs/CLAUDE.md` → 0 lines; `diff docs/archive/CLAUDE.md CLAUDE.md` → 226 lines; `grep -c "CLAUDE.md" docs/README.md` → 0 (root CLAUDE.md is outside `docs/` entirely and is CHIP-PROTOCOL's own do-not-touch file). |
| `docs/archive/DEPLOYMENT.md` (1499 bytes) | doc duplicate | n/a | n/a | n/a | **REFERENCED indirectly, and diverged from the real canonical doc** — identical to `docs/DEPLOYMENT.md` (0 diff lines) but 621 diff lines from `docs/workflows/DEPLOYMENT.md`, which is the one actually linked from `docs/README.md` ("⚠️ STAGING-FIRST") and cited by 3 live `.claude/skills/*.md` files. | `diff docs/archive/DEPLOYMENT.md docs/DEPLOYMENT.md` → 0; `diff docs/archive/DEPLOYMENT.md docs/workflows/DEPLOYMENT.md` → 621; `grep -rln "docs/workflows/DEPLOYMENT\.md" --include="*.md" .` → `.claude/skills/deploy-staging.md`, `.claude/skills/session-start.md`, `.claude/skills/deploy-prod.md`. `grep -rln "docs/DEPLOYMENT\.md" --include="*.md" .` (the flat/archive one, substring-distinct from `docs/workflows/DEPLOYMENT.md`) → empty, zero referrers. |
| `docs/archive/REFACTORING.md` (5302 bytes) and `docs/archive/phase1.md` (18740 bytes) | **archive-only, no live counterpart anywhere** | n/a | n/a | n/a | **unreferenced — all four searches clean, AND present only in archive** (this answers the prompt's specific question 5). `find . -name REFACTORING.md -not -path "*/docs/archive/*"` and same for `phase1.md` both return nothing — no live copy exists at all, unlike the other 12 archive files. Their subject (the app.js→app-refactored.js split, and the original Phase-1 project brief) is NOT the same content as `docs/history/SERVICE-EXTRACTION-2025-09.md` (`diff` → 670 lines, unrelated text) — so these two are not a rename/reorg of an existing history doc, they are literally orphaned. | `find . -name "REFACTORING.md" -not -path "*/node_modules/*" -not -path "*/docs/archive/*"` → empty. `find . -name "phase1.md" -not -path "*/node_modules/*" -not -path "*/docs/archive/*"` → empty. `diff docs/archive/REFACTORING.md docs/history/SERVICE-EXTRACTION-2025-09.md \| wc -l` → 670. |
| **Bonus finding, doc-vs-repo contradiction**: `docs/README.md` links two files that do not exist | doc bug | n/a | n/a | n/a | n/a | `docs/README.md` "History" section links `history/COMPONENT-PHASES.md` and `history/MAJOR-MIGRATIONS.md`; `docs/history/` contains only `SERVICE-EXTRACTION-2025-09.md`. Both linked files are 404s in the repo. | `ls docs/history/` → only `SERVICE-EXTRACTION-2025-09.md`; `ls docs/history/COMPONENT-PHASES.md docs/history/MAJOR-MIGRATIONS.md` → both "No such file or directory". |
| `TASK_CATEGORY_TAGS` / `TASK_INSTITUTION_TAGS` in `client/src/js/utils/constants.js:6,13` — **the CLAUDE.md self-contradiction candidate** | resolved contradiction | only their own `export const` lines | n/a | n/a | n/a | **RESOLVED, with CLAUDE.md itself found to be wrong in one place**: `grep -rn "TASK_CATEGORY_TAGS\|TASK_INSTITUTION_TAGS" --include="*.js" --include="*.html" .` → only `constants.js:6` and `:13` (their own definitions), zero consumers anywhere. The actual Tag+Institution filter dropdowns on the Tasks tab (`client/src/js/components/tabs/TasksTab.js:57,64`) iterate `systemCategoryTags`/`systemInstitutionTags` — Alpine state populated from the `Tag` Prisma model via `loadSystemTags()` (`app-refactored.js:5048-5049` at time of read; that file is being actively rewritten by a sibling chip so line numbers may drift). **CLAUDE.md line 133 is correct**: "the legacy `TASK_CATEGORY_TAGS`/`TASK_INSTITUTION_TAGS` arrays ... are unused." **CLAUDE.md line 139 is wrong/stale**: its last sentence reads "Tag + Institution filter dropdowns on the Tasks tab filter client-side via `getFilteredTasks()`; constants live in `client/src/js/utils/constants.js` (`TASK_CATEGORY_TAGS`, `TASK_INSTITUTION_TAGS`)" — this describes the pre-Tag-model implementation and was never updated after the migration to the DB-backed `Tag` model described earlier in the same file. The dropdowns DO filter via `getFilteredTasks()` (that half is right) but do NOT source their options from the constants.js arrays (that half is wrong). Recommend the Integrator delete the trailing clause of line 139 (`; constants live in ...TASK_INSTITUTION_TAGS\`)`) as a doc fix. | `grep -n "TASK_CATEGORY_TAGS\|TASK_INSTITUTION_TAGS" client/src/js/utils/constants.js CLAUDE.md`; `grep -rn "systemCategoryTags\|systemInstitutionTags" --include="*.js" client/` → app-refactored.js:446-447,5048-5049,5088,5216-5217,5333 and TasksTab.js:57,64, TaskDetailPanel.js:380,400, TaskModal.js:128,151, GoalModal.js:70. |
| `criticalBlock` / `advancedAccordion` in `client/src/js/components/tabs/proforma/helpers.js:125,145` (file is 390 lines total) | unused exports | only their own definitions | n/a | n/a | n/a | **unreferenced — all four searches clean**, confirms CLAUDE.md's own claim ("the older `criticalBlock`/`advancedAccordion` helpers in `helpers.js` still exist but are unused"). | `grep -rn "\bcriticalBlock\b\|\badvancedAccordion\b" --include="*.js" --include="*.html" .` → only `helpers.js:122,125,139,145` (definitions + their own comment headers), no call sites in any other proforma card file. |
| `client/src/js/components/tabs/NewsTab_backup.js` (1 line: two comments, no code, no exports) | orphan stub file | only self-hit | none | none | none | **unreferenced — all four searches clean, trivial to remove** | File content is literally `// Backup of original NewsTab.js before sidebar redesign` + `// This is a backup file created during the sidebar redesign process` — no exports, no logic. `grep -rn "NewsTab_backup" --include="*.js" --include="*.html" .` → zero hits anywhere, not even in docs. |
| `client/src/js/components/tabs/proforma/ProductionPulse.js` (313 lines) — exports `productionPulse()` at line 236, plus internal helpers `_stockSeries`, `_monthTotals`, `_axisGridlines`, `_stripeLabel`, `_hempStripe`, `_grapheneStripe`, `_stockStripe`, `_hoverOverlay` | **orphan module, but LIVE DOC CLAIMS IT IS WIRED IN — doc/code contradiction, not a trivial dead-code case** | only self-hits | none | none | none | **unreferenced by import/call anywhere in JS/HTML — but a *live, non-archive* doc says otherwise, so this needs a human, not a deletion** | `grep -rn "productionPulse" --include="*.js" --include="*.html" .` → only its own `export function productionPulse()` line. `grep -rn "ProductionPulse"` (path/filename form) → only `docs/features/PROFORMA-SYSTEM.md:23,170,209`. `client/src/js/components/tabs/proforma/ProductionSection.js:1` imports only `{ numInput, formGrid, card, sectionHeader, HELP } from './helpers.js'` — no import of `ProductionPulse.js` at all. **`docs/features/PROFORMA-SYSTEM.md:170` (a live, non-archive doc) states:** "**Production** — keeps the Material-flow visualization (`ProductionPulse.js`) between the section header and the Critical block." That is either stale documentation (feature superseded, doc never updated) or a real wiring regression (the visualization was meant to render and doesn't). CLAUDE.md's own Gotchas entry on `ProductionTimeline.js` describes a *different*, later "single combined table" (gantt + production detail) as the current Production sub-tab implementation, which is consistent with `ProductionPulse.js` having been superseded and orphaned when the tab was redesigned, but I could not find a doc or commit explicitly saying so — flagging rather than asserting. |

## Reflections

| Severity | Finding | Where | Status |
|---|---|---|---|
| medium | `client/src/js/app.js` and `app-original.js` are byte-identical, 828-line orphans with zero references anywhere except historical narrative docs | `client/src/js/app.js`, `client/src/js/app-original.js` | left, why: not owned — Command Center rules; evidence in table above |
| medium | `DealModal.js` (89L) and `DealDetailPanel.js` (160L) are confirmed dead — CLAUDE.md's own claim verified true | `client/src/js/components/modals/DealModal.js`, `DealDetailPanel.js` | left, why: not owned — verified, safe per CLAUDE.md's own documentation |
| low | `NewsTab_backup.js` is a 1-line comment-only stub, zero references | `client/src/js/components/tabs/NewsTab_backup.js:1` | left, why: not owned — trivially removable |
| **high** | `ProductionPulse.js` (313 lines, exports `productionPulse()`) is completely unreferenced by any import/call site in source or in the compiled `dist-matrixwrite/` bundle, **yet a live (non-archive) doc, `docs/features/PROFORMA-SYSTEM.md:170`, explicitly claims it renders**: "keeps the Material-flow visualization (`ProductionPulse.js`) between the section header and the Critical block." `ProductionSection.js:1` imports only from `./helpers.js`, never from `./ProductionPulse.js`. This is either a stale doc (feature was superseded when `ProductionTimeline.js` consolidated the Production sub-tab, per CLAUDE.md's own Gotchas) or a real wiring regression — a customer/investor-facing chart the docs promise but the app doesn't show. | `client/src/js/components/tabs/proforma/ProductionPulse.js`; contradicted by `docs/features/PROFORMA-SYSTEM.md:23,170,209` | left, why: not owned — proposed as follow-up, see below |
| medium | `criticalBlock`/`advancedAccordion` in `helpers.js:125,145` confirmed unused, matching CLAUDE.md's claim | `client/src/js/components/tabs/proforma/helpers.js:122-160` | left, why: not owned — verified |
| **high** | CLAUDE.md contradicts itself on whether `TASK_CATEGORY_TAGS`/`TASK_INSTITUTION_TAGS` drive the Tasks filter dropdowns. Line 133 (correct, matches code) says they're unused legacy. Line 139's trailing clause (stale, contradicted by code) says the dropdowns source from `constants.js`. Actual dropdowns (`TasksTab.js:57,64`) read `systemCategoryTags`/`systemInstitutionTags`, populated from the `Tag` Prisma model. | `CLAUDE.md:133` vs `CLAUDE.md:139`; code truth at `client/src/js/components/tabs/TasksTab.js:57,64` and `app-refactored.js:5048-5049` (sibling-owned file, line numbers may drift) | left, why: shared doc — drafted fix below |
| medium | 10 of 14 `docs/archive/` files are byte-identical to a `docs/` root copy that is *itself* unlinked from `docs/README.md`'s canonical index — this isn't "one live + one archived," both copies look stale relative to the reorganized docs under `docs/{core-reference,workflows,features,history}/` | `docs/archive/*.md` vs `docs/*.md` — see evidence table | left, why: shared doc dir — proposed as follow-up |
| medium | `docs/archive/REFACTORING.md` and `docs/archive/phase1.md` have **no live counterpart anywhere in the repo** — genuinely archive-only, unlike the other 12 | `docs/archive/REFACTORING.md`, `docs/archive/phase1.md` | left, why: shared doc dir — informational, no action needed (already correctly archived-only) |
| low | `docs/README.md`'s own "History" section links two files that don't exist: `history/COMPONENT-PHASES.md` and `history/MAJOR-MIGRATIONS.md` — `docs/history/` contains only `SERVICE-EXTRACTION-2025-09.md` | `docs/README.md` (History section) | left, why: shared doc — drafted fix below |
| low | 12 of 14 root `test-*.js` scripts have zero references anywhere (not `package.json`, not docs); 2 (`test-graphene-filter.js`, `test-openai.js`) are documented as manual dev commands in live docs | root `test-*.js` | ambiguous — needs a human (see per-file note in evidence table; these are CLI entrypoints, absence of import is expected for the shape, weaker signal than an ES module) |
| low | `cleanup-non-graphene-articles.js` and `generate-pending-summaries.js` are undocumented, unreferenced standalone DB-writing scripts | root `*.js` | ambiguous — needs a human; cannot execute to verify further under D-004/D-005 |
| info | `staging-seed-data.json` and `staging_data.sql` are NOT dead — actively read/written by `server/routes/seed-staging.js`, `scripts/export-data.js`, `scripts/import-to-staging.js` | root data files | REFERENCED — do not remove |
| info | `article-backup-2025-09-09T*.json` (2 files) are one-time output artifacts of `cleanup-non-graphene-articles.js`'s dynamic filename pattern, not read back by any code | root `article-backup-*.json` | left, why: not owned — data artifacts, low removal risk, different category from source code |

### What I saw outside my scope

- A `dist-matrixwrite/` directory exists in the repo root — build output from a sibling chip following the D-010 rule ("each chip builds to its own directory"). I used it as a free corroborating signal: neither `ProductionPulse.js`'s code nor `getDealModalHtml`/`getDealDetailPanelHtml` appear anywhere in the compiled bundle (`grep -c ... dist-matrixwrite/assets/*.js` → 0 for all), which is independent confirmation that these are genuinely unbundled, not just unimported-but-somehow-still-referenced through a path I didn't grep. It's gitignored per D-010 and not something I touched.
- `docs/` has a real structural problem beyond the archive: it contains both a reorganized tree (`core-reference/`, `workflows/`, `features/`, `history/`, indexed by `docs/README.md`) and a flat layer of old top-level files (`docs/CLAUDE.md`, `docs/DEPLOYMENT.md`, `docs/CLAUDE-AI.md`, etc.) that predate the reorg, are unlinked from the index, and happen to also be duplicated again in `docs/archive/`. This looks like the reorg copied files into their new homes but never deleted the originals, and *then* a later archive pass copied the untouched originals into `docs/archive/` without noticing they were already superseded. Three copies of the same stale content in `docs/CHARACTERIZATION-ANALYSIS-V2.md` specifically (root docs/, docs/features/CHARACTERIZATION-ANALYSIS.md, docs/archive/) though the docs/features one might be current and the flat one stale — I did not diff *content correctness*, only byte-identity, so I can't say which of the three is "right," only that two of the three names are dead weight.
- `client/src/js/utils/constants.js` also exports other arrays I did not audit individually (only checked `TASK_CATEGORY_TAGS`/`TASK_INSTITUTION_TAGS` per the assigned candidate) — worth a follow-up sweep of the whole file.
- I did not exhaustively audit all 114 files under `client/src/js/services/` and `client/src/js/components/` — my automated single-line `export function|const|class NAME` scan is mechanical but has known blind spots: it misses multi-line export signatures, `export default`, `export { a, b }` list exports, and any symbol attached dynamically via `window.foo = ...` (which this repo uses in at least the Test Matrix data module, per CLAUDE.md). It surfaced exactly 3 zero-hit names (`productionPulse`, `getDealModalHtml`, `getDealDetailPanelHtml`) out of everything it could see in that shape — I do not have equivalent confidence about the other export shapes in the other ~110 files.

### Risks in what I built

- My "REFERENCED — do not remove" verdicts are only as good as ripgrep's literal string matching across the checked-out tree at the moment I ran each command; a sibling chip rewriting `app-refactored.js` concurrently could shift line numbers I cited (`5048-5049`, `5604`, `5606` etc.) between my read and the Command Center's review. I called this out inline but it bears repeating here: treat cited line numbers in actively-edited files as approximate, re-grep before acting.
- The `docs/archive/` byte-identity claims are hard facts (`diff` returning nothing is unambiguous), but my claim that the flat `docs/*.md` copies are "themselves stale because unlinked from `docs/README.md`" is an inference, not a mechanical fact — a doc can be intentionally unlinked-but-current. I flagged it as such (not marked "unreferenced — all four searches clean") but a human should confirm before touching `docs/`.
- The unused-export automation script only found 3 hits; if the real count is higher (likely, given known blind spots above), this notes file understates the surface. I'm reporting what I found, not a completeness guarantee.

### Proposed follow-up chips

1. **CHIP-DEADCODE-REMOVE** — act on the four fully-verified-dead items (`app.js`, `app-original.js`, `DealModal.js`, `DealDetailPanel.js`, `NewsTab_backup.js`, `criticalBlock`/`advancedAccordion` in `helpers.js`) per §7's preference: un-export or tombstone rather than delete outright. Lane A, sonnet or opus, owns those specific files only (none overlap with W1-APP-DEDUPE's ownership of `app-refactored.js`).
2. **CHIP-PROFORMA-PULSE-AUDIT** — resolve the `ProductionPulse.js` doc/code contradiction: either wire it back into `ProductionSection.js` (if the visualization is supposed to be live) or update `docs/features/PROFORMA-SYSTEM.md` to remove the claim and archive/tombstone the file. This is investor/customer-facing per the "public factual copy" rule (§6) since it's the Proforma tab, so treat with that rigor. Lane B first (confirm intent with a human — was this cut deliberately?), then Lane A. Sonnet is fine for the doc half; whoever does the wiring should be the same tier as other proforma chips.
3. **CHIP-DOCS-DEDUPE** — resolve the `docs/` triple-duplication (root flat files vs `docs/archive/` vs the reorganized `core-reference/workflows/features/history` tree) and fix the two broken links in `docs/README.md`'s History section. Lane B (docs only), sonnet.
4. **CHIP-ROOT-SCRIPTS-AUDIT** — get a human answer on `cleanup-non-graphene-articles.js`, `generate-pending-summaries.js`, and the 12 unreferenced `test-*.js` scripts: still-used manual tools, or safe to move to a `scripts/` or archive folder? Lane B research (ask the human / check `git log` authorship and last-run dates), sonnet.
5. **CHIP-EXPORT-SWEEP-FULL** — a proper unused-export audit of all 114 files in `services/` and `components/`, handling `export default`, list exports, and `window.*` attachment (the blind spots my mechanical pass had). Lane B, probably wants a stronger model than sonnet for the judgment calls around dynamic/`window.*` usage (see harness note below).

### Draft for shared docs

Two small, mechanical text fixes for the Integrator (I do not touch `CLAUDE.md` or `docs/` directly):

- **`CLAUDE.md` line 139**, delete the trailing clause after "...filter client-side via `getFilteredTasks()`": remove `; constants live in \`client/src/js/utils/constants.js\` (\`TASK_CATEGORY_TAGS\`, \`TASK_INSTITUTION_TAGS\`)`. It contradicts the correct statement on line 133 and the actual code (`TasksTab.js:57,64` reads `systemCategoryTags`/`systemInstitutionTags`, not the constants.js arrays).
- **`docs/README.md`**, History section: either create the missing `docs/history/COMPONENT-PHASES.md` and `docs/history/MAJOR-MIGRATIONS.md`, or remove those two bullet links — they currently 404.

### Harness improvements

- The four-search protocol (§7) worked well and was fast on sonnet for single-symbol candidates (DealModal, criticalBlock, etc.) — a few `grep` calls each, clear verdict. It was noticeably slower and less certain for the "root strays" and "unused exports generally" candidates, which are open-ended by nature — those two took the bulk of my time and still ended in "ambiguous, needs a human" for several items. If future chips get an open-ended candidate like #8, it would help to either scope it much narrower (name specific files/directories) or explicitly budget it as a longer pass, because "audit all unused exports across services/ and components/" doesn't have a natural stopping point for a mechanical search — I had to decide myself when "enough" automated coverage was reached.
- Finding `ProductionPulse.js` depended on cross-referencing a live doc (`docs/features/PROFORMA-SYSTEM.md`) against the code — I only thought to check that doc because I'd already built the habit from the constants.js contradiction candidate. A future prompt could explicitly instruct "for every candidate you clear as dead, also grep `docs/` for its filename" as a standing step, since doc-vs-code contradictions were consistently the highest-value output (this chip found two: the constants.js one I was assigned, and the ProductionPulse.js one I wasn't).
- Sonnet was adequate for this task. The mechanical grepping and tabulation is well within its range. The one place I'd flag for a stronger model: judging whether the `docs/` triple-duplication inference ("unlinked from README.md means stale") is sound, and deciding how much unused-export auditing is "enough" for candidate 8 — those were judgment calls about scope and confidence that a stronger model might have handled with more nuance or caught additional blind spots (e.g., `export default` scanning) without being told to.
- No environment traps beyond what's already in CHIP-PROTOCOL.md §9. Did not need to run the app, build, or touch the database — pure grep/diff/find, all read-only, no collisions with sibling chips observed.
